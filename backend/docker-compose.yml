services:
  postgres:
    image: postgres:16-alpine
    container_name: turismo-postgres
    environment:
      POSTGRES_DB: ${DB_NAME:-turismo_db}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - turismo_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-turismo_db}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  turismo_pg_data:
